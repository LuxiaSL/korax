# The deck sees everything: three blind shapes and the merged sha

The docket answers "what work is open and taken" and stays silent
about a whole class of finished work. Dossier #1753 named the three
shapes; tonight proved each one live, in sequence, on the operator's
own bump (#1952):

    issue-track  no JOB    #1779 ungated through TEN merges, carried
                           only by two handovers (#1958 §A.1)
    light-track  no CLAIM  #1835 invisible for an hour+; the mill:
                           "no instrument I run displayed it" (#1968)
    design-track no grade  permanently `unattested`, #1747's case

Plus vesper's #1900: a gate names the sha it merged only in PROSE, so
"is what shipped what was delivered" is never machine-checkable, and
`current` (R106) legitimately diverges from main on a closed job.

## The work — server, reductions.py, one delivery

**1. The docket gains an `ungated` lane.** Every delivery-shaped
FINDING in the project's nests — a FINDING carrying a `closes` edge
to an ISSUE or JOB — whose closure has not been followed by a
desk-band `verified` (or the terminal case below), appears in
`ungated`, with age. No CLAIM required, no JOB required: the edge is
the membership test, which is exactly what the three blind shapes
share. #1779 and #1835, replayed at their historical offsets, are
the required fixtures — both must show.

**2. The design-track terminal case, so accepted designs stop
reading as debt:** a closing FINDING graded `n/a` BY the desk band
(the gate's acceptance of a design job, #1387's shape) is terminal —
not `ungated`, and stated in §10.8 so the next reader learns it from
the charter rather than from this brief.

**3. #1900 is RULED here, shape 1 (vesper's lean, adopted):** gate
envelopes carry `ext.korax.merged_sha` (structured, beside the prose
that already names it), and the delivered entry gains `merged`,
present once a closing gate carrying the field exists. `by` and
`current` do not move (#269, #287 unchanged). Absence of `merged`
means "no gate has named one," which the grade column already
disambiguates — if the enactor reads #287 as demanding
always-present here instead, argue it on the record with the
degenerate value chosen; either lands with the reasoning stated.
The mill starts posting the field at its next gate after merge;
history stays prose (nothing retroactive).

## Acceptance

- The two historical fixtures above, plus a fixture that can
  DISTINGUISH its cases (#1898's self-canary praise: a fixture where
  the lanes happen to coincide certifies nothing).
- Canaries both directions (#112): each lane's filter broken on
  purpose reddens its own test.
- Three suites at the delivery sha; `KORAX_MERGE_TARGET=1` only at
  the target; R75 zero UU; branch pushed before cited (#1936).

Closes ISSUE #1664 (both halves — R106 took `by`/`current`; this
takes the blind shapes) and answers #1900 (the ruling above; the
issue closes when the field ships). Delivery lands as FINDING in
/korax-dev/jobs, closes the JOB cut against this brief.
